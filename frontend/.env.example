# Frontend env (Create React App). When deployed to Vercel the same domain
# serves both the SPA and the serverless API, so REACT_APP_API_BASE=/api works
# out of the box. For local development against a deployed API set it to the
# full origin, e.g. https://your-app.vercel.app/api.

REACT_APP_API_BASE=/api
