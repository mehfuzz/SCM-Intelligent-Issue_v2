{
  "name": "scm-intelligent-issue",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Airtel SCM Issue Intelligence & Workflow Management Portal — Vercel serverless backend + React frontend, Supabase database.",
  "scripts": {
    "vercel-build": "cd frontend && npm install --legacy-peer-deps && DISABLE_ESLINT_PLUGIN=true CI=false GENERATE_SOURCEMAP=false npm run build",
    "dev:frontend": "cd frontend && npm start",
    "db:reset": "supabase db reset",
    "db:push": "supabase db push"
  },
  "dependencies": {
    "bcryptjs": "^2.4.3",
    "oracledb": "^6.10.0"
  },
  "engines": {
    "node": "22.x"
  }
}
